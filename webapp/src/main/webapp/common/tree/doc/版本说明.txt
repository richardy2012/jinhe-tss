控件名称：单选/多选树

功能说明：	1、单选/多选树的显示，重载
		2、枝节点的打开关闭（响应鼠标事件、接口对象提供方法）
		3、兄弟节点间的移动位置（响应鼠标事件、接口对象提供方法）
		4、节点的增、删、改
		5、节点选择状态的更改（响应鼠标事件、接口对象提供方法），参数控制是否和激活节点相关联
		6、返回选中节点（参数控制是否包括半选节点），返回对象或数组，提供方法转换成xml
		7、TreeNode接口对象。

使用方法：见“树控件文档.txt”、‘树XML模板规范.xml’（参考test.html文件）
名称空间：Tree
作　　者：scq
时　　间：2005-10-29

版本号：partialTree0.9.6

主要更新：
	添加disable、enable功能，禁止、取消全部节点改变选中状态功能
        添加新增节点时是否自动将焦点移到新增节点、是否默认打开指定或第一个有效节点、是否默认激活第一个有效数据节点等属性

修改历史：
2006-4-8
partialTree0.9.6
	添加自定义图标
	EventFirer方法完善，增加处理function类型
	增加跨树的拖动（尚未完善，主要是拖动插入点的控制未处理）
		- onTreeNodeMoved事件增加event.moveTree属性标记拖动的来源树
		- 增加moveExternalTreeNode方法
		- 修正setNodeState方法当state为空时报错问题

2005-10-26：
partialTree0.9.5
	添加节点选中前、激活前、以及选中且激活前触发事件，
	以上事件都可以通过设置event.returnValue = false来阻止激活或选中
2005-9-24：
partialTree0.9.4
	多选树时，添加点击选中节点时，如果按下shift键，则只选中此节点而不选中其所有子节点
	默认选中功能中添加参数，可以控制是否清除原先选中的节点，默认为保留原先选中节点
	添加获取树节点Id字符串方法
2005-9-10：
partialTree0.9.3
	添加方法获取当前节点及以下节点的全部、选中、全选状态节点的id列表字符串
	修改禁用、启用方法，禁用节点时，可控制是否同时禁用其子节点；启用节点时，可控制是否启用其父节点等
	改造Display对象，解决滚动条不能使用上下箭头的问题，优化显示速度和效率
	修改部分小Bug

2005-4-19：
partialTree0.9.2
	新增默认选中功能的实现方法二（参数为id字符串，多个id用“,”隔开）。
	增加DataSource对象，负责数据装载，支持xml节点、xml字符串、xml文件、xml数据岛

2004-12-23：
partialTree0.9.1
	调整结构，优化性能。新增只选中本身节点（不改变父、子节点的选中状态）参数，新增默认选中功能（多选树参数为xml数据，单选树为节点id）。
	增加DataSource对象，负责数据装载，支持xml节点、xml字符串、xml文件、xml数据岛
2004-6-23：
partialTree0.9
	重新组织结构，TreeNode对象只负责xml节点操作，Display负责处理页面生成和显示，初始化的时候一次生成表格所有的行，并且为每一行绑定对象Row，控件滚动显示不同的节点的时候，此对象只是将新的节点数据给对应的Row对象。Row主要负责节点的数据显示的行中。Tree对象负责组织控件的所有属性，和处理因属性不同引起的不同之处，如（treeType，处理单选、多选树的不同之处）。此版本为了调整结构，把与上述对象联系不紧密的Search对象（查询功能）去除了。
2004-6-21：
partialTree0.8
	此版本将显示的节点的TreeNode对象绑定在tr对象上，减少了每次显示时TreeNode的初始化时间，优化性能。

2004-6-21：
partialTree0.7
	此版本增加了查询功能。

2004-6-18：
partialTree0.6
	将滚动延时进一步细化，节点文字和显示树结构的线条不作延时显示处理，图片仍做延时显示处理，效果不好。（此版本因性能不好，没有进一步开发）

2004-6-11：
partialTree0.5
	为了进一步改善显示效果，用图替换伸缩状态和选择状态的文字，显示效果大大改善，性能几乎没有影响。

2004-6-11：
partialTree0.4
	增加canSelected属性，控制节点是否可以被选中和激活（高亮）。
	制表符由层＋样式表替代，显示效果大大改观。

2004-6-7：
partialTree0.3
	此版本使用表格进行显示，用来显示树结构的线条由制表符替代，只显示可见范围内的节点，大大减少了渲染的层对象，性能大大提升，但显示效果有所欠缺。

2004-5-28：
partialTree0.2
	此版本使用文字＋样式表，替代所有上一版本中使用的图片，性能有所提升，但是，每次拖动滚动条时仍会产生停顿。

2004-5-25：
partialTree0.1
	从totalTree0.4修改成此版本，实现树的分页显示，即只将需要显示的节点显示在页面上，看不到的节点不做渲染。性能比totalTree0.4版大有提升。但是，每次拖动滚动条时会产生停顿。
	此版本仍沿用totalTree0.4的方法，所有节点的位置都是渲染的，节点中的元素没有被渲染。
